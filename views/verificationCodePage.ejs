<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enter Verification Code</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../stylesSheets/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-gradient-to-r from-blue-500 to-green-500 flex justify-center items-center h-screen" >
    <% if (error_msg) { %>
      <div class="alertMessage">
        <%= error_msg %>
      </div>
      <% } %>
    <div class="bg-white shadow-md rounded-md p-6 w-full max-w-sm">
        <h2 class="text-2xl font-bold text-gray-700 text-center mb-4">Enter Verification Code</h2>
        <form id="codeForm" method="post" action="/owner/verifyCode" class="space-y-4">
            <label for="code" class="block text-gray-600 font-medium mb-2">Verification Code</label>
            <input
                type="text"
                id="code"
                name="code"
                placeholder="Enter your code"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
            <button
                type="submit"
                class="w-full bg-blue-500 text-white py-2 mt-4 rounded-md hover:bg-blue-600 transition"
            >
                Submit Code
            </button>
        </form>
    </div>
</body>
<script src="../javaScripts/script.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>
